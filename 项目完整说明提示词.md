# 薅了个毛（Haloy Wool）— 完整项目提示词

> 本文件记录了从零搭建「薅了个毛」微信小程序 + Java 后台的全部内容，可作为后续开发、维护、AI 辅助的完整上下文。

---

## 一、项目概述

**项目名称**：薅了个毛（Haloy Wool）
**Slogan**：本来不想薅的，但它实在太便宜了
**类型**：薅羊毛信息社区 — 微信小程序 + Java 后台
**功能**：用户发布/浏览薅羊毛优惠信息，管理员审核，积分激励机制

### 角色权限

| 角色 | 能做什么 |
|------|----------|
| 游客（未登录） | 浏览已上线信息列表（看不到详情） |
| 普通用户（微信登录） | 查看详情、发布/编辑/删除自己的信息、批量导入、积分兑换 |
| 管理员 | 审核信息、上线/下线/删除任意信息、管理所有内容 |

### 业务流程

```
用户微信登录 → 自动注册 → 发布薅羊毛信息 → 状态"待审核"
                                              ↓
                        管理员审核通过 → "已上线" + 发布者获得 1 积分
                        管理员驳回   → "已驳回" + 附带理由
                                              ↓
                        用户可用积分兑换商品（话费券/京东卡/视频会员等）
```

### 信息状态流转

```
待审核(0) ──管理员通过──→ 已上线(1)
    │
    └──管理员驳回──→ 已驳回(2)
                        │
                        └──管理员上线──→ 已上线(1)

已上线(1) ──管理员下线──→ 已下线(3)
已下线(3) ──管理员上线──→ 已上线(1)
已驳回(2) ──管理员上线──→ 已上线(1)
```

---

## 二、技术栈

### 后台（Java）

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 8+ | 编程语言 |
| Spring Boot | 2.7.18 | Web 框架 |
| MyBatis-Plus | 3.5.5 | 数据库 ORM |
| MySQL | 5.7+ / 8.0+ | 数据库 |
| JWT (jjwt) | 0.11.5 | 登录凭证 |
| EasyExcel | 3.3.4 | Excel 批量导入 |
| Hutool | 5.8.25 | 工具库 |
| Maven | 3.6+ | 项目构建 |

### 前端（微信小程序）

| 技术 | 说明 |
|------|------|
| 微信小程序原生框架 | WXML + WXSS + JS |
| 自定义组件 | 空状态组件、广告位占位组件 |
| 全局状态管理 | app.globalData |

---

## 三、项目结构

### 后台（HLGM-JAVA-SERVER）

```
HLGM-JAVA-SERVER/
├── pom.xml
├── sql/init.sql                          ← 数据库建表脚本
├── README.md
└── src/main/
    ├── resources/
    │   └── application.yml               ← 配置文件
    └── java/com/wool/
        ├── WoolApplication.java          ← 启动入口
        ├── common/
        │   ├── R.java                    ← 统一返回格式
        │   ├── BizException.java         ← 业务异常
        │   ├── GlobalExceptionHandler.java
        │   ├── Constants.java            ← 常量
        │   ├── WoolStatus.java           ← 状态枚举
        │   └── PointsChangeType.java     ← 积分类型枚举
        ├── config/
        │   ├── WebMvcConfig.java         ← 拦截器+跨域
        │   ├── MybatisPlusConfig.java    ← 分页插件
        │   └── MyMetaObjectHandler.java  ← 自动填充时间
        ├── entity/
        │   ├── User.java
        │   ├── WoolInfo.java
        │   ├── PointsLog.java
        │   ├── ExchangeGoods.java
        │   └── ExchangeRecord.java
        ├── mapper/
        │   ├── UserMapper.java
        │   ├── WoolInfoMapper.java
        │   ├── PointsLogMapper.java
        │   ├── ExchangeGoodsMapper.java
        │   └── ExchangeRecordMapper.java
        ├── dto/
        │   ├── WxLoginDTO.java
        │   ├── WoolInfoDTO.java
        │   ├── WoolInfoImportDTO.java    ← Excel导入映射
        │   ├── AuditDTO.java
        │   └── ExchangeDTO.java
        ├── vo/
        │   ├── LoginVO.java
        │   ├── WoolInfoVO.java
        │   ├── PointsLogVO.java
        │   └── ImportResultVO.java       ← 导入结果
        ├── util/
        │   └── JwtUtil.java
        ├── interceptor/
        │   ├── AuthInterceptor.java      ← 登录验证
        │   └── AdminInterceptor.java     ← 管理员验证
        ├── service/
        │   ├── AuthService.java
        │   ├── WoolInfoService.java
        │   ├── PointsService.java
        │   ├── ExchangeService.java
        │   └── impl/
        │       ├── AuthServiceImpl.java
        │       ├── WoolInfoServiceImpl.java
        │       ├── PointsServiceImpl.java
        │       └── ExchangeServiceImpl.java
        └── controller/
            ├── AuthController.java
            ├── WoolInfoController.java
            ├── AdminController.java
            ├── PointsController.java
            └── ExchangeController.java
```

### 前端（hlgm-miniprogram）

```
hlgm-miniprogram/
├── app.js                                ← 全局逻辑（登录、状态）
├── app.json                              ← 全局配置（路由、TabBar）
├── app.wxss                              ← 全局样式（主题色 #FF6B35）
├── project.config.json
├── sitemap.json
├── images/
│   ├── logo.png / logo-120/200/400.png   ← Logo 多尺寸
│   ├── splash-logo.png                   ← 开屏页 Logo
│   └── tab-home/add/mine(-active).png    ← TabBar 图标（线条风格）
├── utils/
│   ├── request.js                        ← HTTP 封装
│   ├── auth.js                           ← 登录鉴权
│   └── util.js                           ← 工具函数
├── components/
│   ├── empty/                            ← 空状态组件
│   └── ad-banner/                        ← 广告位占位组件（5处预留）
└── pages/
    ├── welcome/       🎬 开屏页           Logo + Slogan + 进入按钮
    ├── index/         🏠 首页             卡片列表 + 搜索 + 广告位
    ├── detail/        📄 详情页           富文本 + 权限按钮 + 广告位
    ├── publish/       ✏️ 发布页           表单 + Excel批量导入
    ├── mine/          👤 个人中心         管理员区分 + 登录弹窗 + 积分
    ├── admin/         ⚙️ 审核管理         审核通过/驳回 + 上下线
    ├── exchange/      🎁 积分兑换         建设中占位
    ├── points/        📊 积分明细         积分变动记录
    └── exchange-record/ 📋 兑换记录       占位页
```

---

## 四、数据库设计（5 张表）

```sql
CREATE DATABASE IF NOT EXISTS wool_db DEFAULT CHARACTER SET utf8mb4;

-- 1. 用户表
CREATE TABLE `t_user` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `openid`      VARCHAR(64)  NOT NULL COMMENT '微信openid',
    `nickname`    VARCHAR(64)  DEFAULT '',
    `avatar_url`  VARCHAR(512) DEFAULT '',
    `role`        TINYINT      NOT NULL DEFAULT 0 COMMENT '0=普通用户, 1=管理员',
    `points`      INT          NOT NULL DEFAULT 0,
    `status`      TINYINT      NOT NULL DEFAULT 1 COMMENT '0=禁用, 1=正常',
    `created_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 2. 羊毛信息表
CREATE TABLE `t_wool_info` (
    `id`            BIGINT        NOT NULL AUTO_INCREMENT,
    `user_id`       BIGINT        NOT NULL,
    `title`         VARCHAR(128)  NOT NULL,
    `content`       TEXT          NOT NULL,
    `category`      VARCHAR(32)   DEFAULT '',
    `source_url`    VARCHAR(512)  DEFAULT '',
    `claim_steps`   TEXT          DEFAULT NULL,
    `status`        TINYINT       NOT NULL DEFAULT 0 COMMENT '0=待审核, 1=已上线, 2=驳回, 3=已下线',
    `reject_reason` VARCHAR(256)  DEFAULT '',
    `view_count`    INT           NOT NULL DEFAULT 0,
    `created_at`    DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`    DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_status` (`status`),
    KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. 积分变动记录表
CREATE TABLE `t_points_log` (
    `id`           BIGINT       NOT NULL AUTO_INCREMENT,
    `user_id`      BIGINT       NOT NULL,
    `change_type`  TINYINT      NOT NULL COMMENT '1=发布奖励, 2=兑换扣减, 3=管理员调整',
    `change_value` INT          NOT NULL,
    `before_points` INT         NOT NULL,
    `after_points`  INT         NOT NULL,
    `remark`       VARCHAR(128) DEFAULT '',
    `biz_id`       BIGINT       DEFAULT NULL,
    `created_at`   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 4. 兑换商品表
CREATE TABLE `t_exchange_goods` (
    `id`          BIGINT        NOT NULL AUTO_INCREMENT,
    `name`        VARCHAR(128)  NOT NULL,
    `description` VARCHAR(512)  DEFAULT '',
    `image_url`   VARCHAR(512)  DEFAULT '',
    `points_cost` INT           NOT NULL,
    `stock`       INT           NOT NULL DEFAULT 0,
    `status`      TINYINT       NOT NULL DEFAULT 1,
    `created_at`  DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 5. 兑换记录表
CREATE TABLE `t_exchange_record` (
    `id`          BIGINT        NOT NULL AUTO_INCREMENT,
    `user_id`     BIGINT        NOT NULL,
    `goods_id`    BIGINT        NOT NULL,
    `goods_name`  VARCHAR(128)  NOT NULL,
    `points_cost` INT           NOT NULL,
    `status`      TINYINT       NOT NULL DEFAULT 1,
    `created_at`  DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 初始商品
INSERT INTO t_exchange_goods (name, description, points_cost, stock, status) VALUES
('5元话费券', '满10元可用话费券', 10, 100, 1),
('10元京东E卡', '京东购物卡', 20, 50, 1),
('视频会员月卡', '爱奇艺/优酷月卡任选', 30, 30, 1);
```

---

## 五、API 接口文档

### 通用说明

**统一返回格式：**
```json
{ "code": 0, "msg": "success", "data": { ... } }
```

**错误码：** 0=成功, 1=业务错误, 2=参数校验失败, 401=未登录, 403=权限不足, 500=服务器错误

**登录认证：** 请求头 `Authorization: Bearer <token>`，有效期 7 天

**分页参数：** `pageNum`(默认1) `pageSize`(默认10)

### 接口列表

| # | 方法 | 路径 | 说明 | 需登录 | 需管理员 |
|---|------|------|------|--------|----------|
| 1 | POST | `/api/auth/login` | 微信登录 | ❌ | ❌ |
| 2 | GET | `/api/wool/list` | 信息列表（公开） | ❌ | ❌ |
| 3 | GET | `/api/wool/detail/{id}` | 信息详情 | ✅ | ❌ |
| 4 | POST | `/api/wool/publish` | 发布信息 | ✅ | ❌ |
| 5 | PUT | `/api/wool/update/{id}` | 修改信息 | ✅ | ❌ |
| 6 | DELETE | `/api/wool/delete/{id}` | 删除信息 | ✅ | ❌ |
| 7 | GET | `/api/wool/mine` | 我的发布 | ✅ | ❌ |
| 8 | POST | `/api/wool/import` | 批量导入 | ✅ | ❌ |
| 9 | GET | `/api/admin/wool/list` | 管理员列表 | ✅ | ✅ |
| 10 | POST | `/api/admin/wool/audit/{id}` | 审核 | ✅ | ✅ |
| 11 | PUT | `/api/admin/wool/online/{id}` | 上线 | ✅ | ✅ |
| 12 | PUT | `/api/admin/wool/offline/{id}` | 下线 | ✅ | ✅ |
| 13 | DELETE | `/api/admin/wool/delete/{id}` | 管理员删除 | ✅ | ✅ |
| 14 | GET | `/api/points/balance` | 积分余额 | ✅ | ❌ |
| 15 | GET | `/api/points/log` | 积分记录 | ✅ | ❌ |
| 16 | GET | `/api/exchange/goods` | 商品列表 | ✅ | ❌ |
| 17 | POST | `/api/exchange/do` | 兑换商品 | ✅ | ❌ |
| 18 | GET | `/api/exchange/records` | 兑换记录 | ✅ | ❌ |

---

## 六、已知问题与修复记录

### 问题1：首页列表查不到审核通过的信息

**原因**：`listOnline` 使用 `LambdaQueryWrapper` + 方法引用，在某些 MyBatis-Plus 版本下有兼容性问题

**修复**：改用 `QueryWrapper` + 字符串列名

```java
// 修复前
LambdaQueryWrapper<WoolInfo> wrapper = new LambdaQueryWrapper<WoolInfo>()
        .eq(WoolInfo::getStatus, WoolStatus.ONLINE.code)
        .like(StringUtils.hasText(keyword), WoolInfo::getTitle, keyword)
        .orderByDesc(WoolInfo::getCreatedAt);

// 修复后
QueryWrapper<WoolInfo> wrapper = new QueryWrapper<>();
wrapper.eq("status", WoolStatus.ONLINE.code)
       .like(StringUtils.hasText(keyword), "title", keyword)
       .orderByDesc("created_at");
```

**文件**：`WoolInfoServiceImpl.java` → `listOnline` 方法

### 问题2：编辑按钮点击没反应

**原因**：编辑时把整个 content（可能几千字）拼进 URL 参数，URL 过长导致跳转失败

**修复**：改用 `app.globalData.editData` 传递编辑数据

```javascript
// detail.js - 编辑时存入 globalData
onEdit() {
  const info = this.data.info;
  app.globalData.editData = {
    id: info.id, title: info.title, content: info.content,
    category: info.category, sourceUrl: info.sourceUrl, claimSteps: info.claimSteps
  };
  wx.navigateTo({ url: '/pages/publish/publish?mode=edit' });
}

// publish.js - 从 globalData 读取
onLoad(options) {
  if (options.mode === 'edit') {
    const editData = app.globalData.editData;
    if (editData) {
      this.setData({ isEdit: true, editId: editData.id, form: { ... } });
      app.globalData.editData = null;
    }
  }
}
```

**文件**：`detail.js`、`publish.js`

### 问题3：发布页输入框圆角过大

**修复**：`publish.wxss` 中 `.form-input` 和 `.form-textarea` 的 `border-radius` 从 `44rpx` 改为 `6rpx`

### 问题4：微信登录失败

**原因**：`application.yml` 中 `wechat.appid` 和 `wechat.secret` 未填写真实值

**修复**：填入真实的微信小程序 AppID 和 AppSecret

### 问题5：个人中心未区分管理员

**修复**：mine 页面增加管理员专属网格入口（待审核/已上线/全部/已驳回），管理员显示金色「👑 管理员」标签

### 问题6：微信头像昵称未获取

**修复**：登录流程改为弹窗模式，使用 `open-type="chooseAvatar"` + `<input type="nickname">` 获取微信头像和昵称

---

## 七、配色方案

| 元素 | 色值 | 用途 |
|------|------|------|
| 主题色 | `#FF6B35` | 按钮、导航栏、TabBar 选中 |
| 渐变辅色 | `#FF8C5A` | 渐变背景终点 |
| 危险色 | `#FF4D4F` | 删除、驳回 |
| 成功色 | `#52C41A` | 通过、已上线 |
| 警告色 | `#FAAD14` | 待审核 |
| 页面背景 | `#F5F5F5` | 全局底色 |
| 卡片背景 | `#FFFFFF` | 卡片、表单 |
| 主文字 | `#333333` | 标题、正文 |
| 辅助文字 | `#666666` | 摘要 |
| 提示文字 | `#999999` | 时间、作者 |
| 分割线 | `#EEEEEE` | 边框、分割 |

---

## 八、前端核心机制

### 登录流程

```
点击「点击登录」
    ↓
弹出弹窗：chooseAvatar + type="nickname"
    ↓
确认登录 → wx.login() 获取 code
    ↓
POST /api/auth/login { code, nickname, avatarUrl }
    ↓
返回 token + 用户信息 → 存入 globalData + localStorage
    ↓
后续请求自动带 Authorization: Bearer <token>
```

### HTTP 请求封装（utils/request.js）

- 自动在 Header 中携带 `Authorization: Bearer <token>`
- `code !== 0` 时自动 toast 错误信息
- 401 状态自动清除登录状态并提示重新登录
- 支持 `showLoading` 参数

### 广告位预留（5 处，当前全部隐藏）

| 位置 | 组件 | 说明 |
|------|------|------|
| 首页顶部 | `<ad-banner position="index_top" />` | 搜索栏下方横幅 |
| 首页列表间 | `<ad-banner position="index_between" />` | 第5条信息后插屏 |
| 详情页底部 | `<ad-banner position="detail_bottom" />` | 内容区下方 |
| 个人中心 | `<ad-banner position="mine_banner" />` | 用户信息下方 |
| 积分页 | `<ad-banner position="points_banner" />` | 积分概览下方 |

广告组件 `showAd` 默认为 `false`（完全隐藏），接入广告 SDK 时改为 `true` 并替换内部实现。

---

## 九、配置文件

### application.yml（后台）

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wool_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 你的MySQL密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

wechat:
  appid: 你的小程序AppID
  secret: 你的小程序AppSecret

jwt:
  secret: 随机字符串至少32位
  expiration: 604800000  # 7天

logging:
  level:
    com.wool: debug
```

### app.js（前端关键配置）

```javascript
App({
  globalData: {
    userInfo: null,
    token: '',
    baseUrl: 'http://localhost:8080'   ← 改成你的后台地址
  }
})
```

### project.config.json（前端）

```json
{
  "appid": "你的小程序AppID",
  "projectname": "薅了个毛"
}
```

---

## 十、启动步骤

### 后台

```bash
# 1. 建库
cd HLGM-JAVA-SERVER/sql
mysql -u root -p < init.sql

# 2. 改配置
# 编辑 src/main/resources/application.yml（数据库密码、微信密钥）

# 3. 编译
cd HLGM-JAVA-SERVER
mvn clean package -DskipTests

# 4. 启动
java -jar target/wool-backend-1.0.0.jar
```

### 前端

1. 微信开发者工具导入 `hlgm-miniprogram` 目录
2. 修改 `app.js` 中 `baseUrl` 为后台地址
3. 修改 `project.config.json` 中 `appid`
4. 编译预览

### 设置管理员

```sql
USE wool_db;
UPDATE t_user SET role = 1 WHERE openid = '目标用户的openid';
```

---

## 十一、积分规则

| 场景 | 积分变动 |
|------|----------|
| 发布信息审核通过 | +1 积分 |
| 积分兑换商品 | -对应积分 |
| 管理员手动调整 | 由管理员决定 |

---

## 十二、前端页面功能清单

| 页面 | 功能 | 对应 API |
|------|------|----------|
| 开屏页 | Logo + Slogan + 进入按钮，首次展示，之后自动跳过 | — |
| 首页 | 分页列表、搜索、下拉刷新、上拉加载、广告位 | `GET /api/wool/list` |
| 详情 | 查看内容、编辑/删除（本人）、审核/上下线（管理员）、广告位 | `GET /api/wool/detail/{id}` |
| 发布 | 填写表单提交、Excel 批量导入 | `POST /api/wool/publish` / `POST /api/wool/import` |
| 个人中心 | 积分展示、我的发布（按状态筛选）、管理员快捷入口、登录弹窗 | `GET /api/wool/mine` |
| 审核管理 | 审核通过/驳回（带理由）、上下线、内容摘要预览 | `GET/POST/PUT/DELETE /api/admin/wool/*` |
| 积分兑换 | 建设中占位 + 订阅通知 | — |
| 积分明细 | 积分变动记录 | `GET /api/points/log` |
| 兑换记录 | 占位页 | — |

---

## 十三、后续可扩展

1. **图片上传**：发布信息时支持上传图片，需要对接 OSS 或微信云存储
2. **积分兑换商城**：对接商品列表、兑换、库存扣减接口
3. **消息推送**：审核结果通知、新信息推送
4. **分享功能**：已预留 `onShareAppMessage`，可扩展海报生成
5. **广告接入**：5 个广告位已预留，接入微信广告 SDK 即可
6. **数据统计**：浏览量、发布量、用户增长等运营数据
