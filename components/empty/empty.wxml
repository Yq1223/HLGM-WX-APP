<view class="empty-wrap">
  <view class="empty-emoji">{{emoji}}</view>
  <view class="empty-title">{{title}}</view>
  <view class="empty-desc" wx:if="{{desc}}">{{desc}}</view>
  <view class="empty-btn" wx:if="{{btnText}}" bindtap="onBtnTap">{{btnText}}</view>
</view>
